# Dupert frontend environment - copy to frontend/.env and fill in real values.
# Vite exposes only VITE_ prefixed variables to the browser bundle.

# Google Maps browser API key for Maps JavaScript rendering.
# Restrict HTTP referrers to localhost + production origins.
VITE_GOOGLE_MAPS_API_KEY=REPLACE_WITH_GOOGLE_MAPS_BROWSER_KEY

# Native iOS Google Maps SDK key. This is embedded in a native app bundle, so
# restrict it to the Dupert iOS bundle ID. Android reads its separate SDK key
# from GOOGLE_MAPS_ANDROID_API_KEY at native-build time instead.
VITE_NATIVE_IOS_MAPS_API_KEY=

# Backend base URL. Leave empty only for web local Vite proxying/same-origin /api.
# Native builds require an explicit absolute URL; staging and production load the
# tracked non-secret endpoints in .env.native-staging/.env.native-production.
# Native development uses an explicit local value from .env.native-development.local.
VITE_BACKEND_API_URL=

# Optional lightweight app wall password. This is bundled into the browser and is
# only a soft gate, not real access control.
VITE_APP_ACCESS_PASSWORD=REPLACE_WITH_SHARED_APP_PASSWORD

# Optional Google Maps vector map id for cloud styling. Not required locally.
VITE_GOOGLE_MAPS_MAP_ID=
