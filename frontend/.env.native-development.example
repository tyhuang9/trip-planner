# Copy to .env.native-development.local and set the reachable backend for your
# simulator/device. Native development permits an explicit http or https URL.
VITE_BACKEND_API_URL=http://localhost:8000
